# Student Budget Expense Tracker

Simple beginner-friendly PHP + MySQL expense tracker for localhost (XAMPP).

## Quick Run
1. Create/import database from `database/expense_tracker.sql`.
2. Put this folder inside `htdocs`.
3. Open `http://localhost/student-expense-tracker/`.
